function websiteTitleChange() {
    if (language == "english") Cout("Author: " + text.author + " edition: " + lastestEdition + "\ngithub profile: " + text.githubLink);
    else if (language == "中文") title.innerHTML = (title.english);
}